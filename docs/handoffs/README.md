# Handoffs

Dated work orders, one file per session-sized piece of work, named
`YYYY-MM-DD-<slug>.md`. Status-tracked in the file's own front matter.

**A handoff without a completion status will be re-executed by a future session.** That is
the whole point of the convention: a session that finds an `open` handoff should assume the
work is not done. So closing one out is not bookkeeping, it is what stops the same analysis
being run twice with different results.

```
**Status:** open
**Status:** complete (PR #NN)
```

## Two rules that keep these useful

**The work order is preserved verbatim, including anything the prose rules would forbid.**
Amending a handoff after execution destroys the record of what was actually asked, which is
the only thing that makes "the data contradicted the brief" a checkable claim rather than an
assertion. Append an outcome section; do not edit the body.

**Where a handoff was wrong, the outcome says so and points at the measurement.** The
amendments under [`../design/`](../design/) carry the detail; the handoff carries the
pointer, so a reader who starts from the work order is not left with the premises it opened
with.

| Handoff | Status |
|---|---|
| [2026-08-01-flow-decomposition.md](2026-08-01-flow-decomposition.md) | **complete, closed out 2026-08-02 as §10.** Layer 3, flow decomposition and fragility-weighted exit size. Landed as `0917eb1` with **no PR**, predating the PR workflow. §9's premise 4 is now closed (the appendix surfaced and is executed in `tests/test_appendix.py`); its §6 cocoa answer was sharpened by `2026-08-02 §B28`. Its one open item, the duplicated `decompose`, was **measured and pinned as `2026-08-02 §B29`**: `cotdata`'s is this one at `tolerance=1.0` with gaps off, 100.000000% agreement. **That decision has since been made**: cotdata dropped its copy in cotdata#93, so `cotdata.vintage_flow.decompose` no longer exists and `tests/test_flow_equivalence.py` skips rather than comparing. `zero_sum_check` stays in cotdata, being a claim about its own parse, and `futures/cot_adapter.py` runs it on every load. **Nothing open** |
| [2026-08-02-reflexivity.md](2026-08-02-reflexivity.md) | **COMPLETE.** §A.8 cascade amplification, shipped as `futures/reflexivity.py`. The horizon decision it was blocked on was dissolved rather than answered: `g` is a staircase, so no horizon is picked. Corrections in `2026-08-02 §B13-B15` |
| [2026-08-02-validation-prereg.md](2026-08-02-validation-prereg.md) | **EXECUTED 2026-08-01, verdict `uninformative`.** §10 pre-registration, run in `npf` by a cold session. Outcome appended as §9, crowdmon-facing findings as `2026-08-02 §B17`. §7.8 deferred, re-check 2026-11-01. **The clean episodes are spent** |
| [2026-08-02-macro-book-pca.md](2026-08-02-macro-book-pca.md) | **complete (PR #21).** `futures/macro_pca.py`. PC1 is the grain complex on Disaggregated and the macro book on TFF (`B21`), so the report type is the subject rather than a parameter. Reaches 2008; no episode examined, deliberately |
| [2026-08-02-coverage-reporting.md](2026-08-02-coverage-reporting.md) | **complete (PR #15), corrected twice (PR #17, PR #19), closed out 2026-08-02.** `futures/coverage.py`, keyed on `market_code`. `2026-08-02 §B18` found the ladder skipped the three terms `D` is built from, and that it is not monotonic because `phi` is price-free. Close-out added the missing third correction and `§B30`: the two unscoreable markets are one migrated instrument, and merging them end to end lifts every rung and changes no verdict, so **2 of 27 is really 1 of 26** and lumber is genuinely unscoreable |
| [2026-08-02-roll-congestion.md](2026-08-02-roll-congestion.md) | **COMPLETE.** `futures/roll.py`. All three components of spec §379 are blocked, including OI migration: the price frame's `Open Interest` is whole-market. What shipped is roll-window volume and its effect on `pressure.T`, which is 5.1% and wrong-signed for five of sixteen markets. `2026-08-02 §B19` |
| [2026-08-02-trend-alignment.md](2026-08-02-trend-alignment.md) | **COMPLETE.** Spec §368, shipped as `futures/alignment.py`. The score **cannot reach 1**: the blend is heavily tied, so the ceiling averages 0.931 and runs 0.340 to 0.969, and the raw figure is not comparable across weeks. No warm-up, so it is the earliest-starting engine here. `2026-08-02 §B20`
| [2026-08-02-correlation-clustering.md](2026-08-02-correlation-clustering.md) | **COMPLETE.** Spec §369, shipped as `futures/clustering.py`. Its own JPY-energy example is **not in the data**; the real cluster is `{6J, ZB, ZF, ZN, ZT}`, the yen carry, which the partition finds on its own at k=8. `2026-08-02 §B25` |
| [2026-08-02-template-followups.md](2026-08-02-template-followups.md) | **COMPLETE.** Five measurements and two doc corrections, `2026-08-02 §B33-§B37`. Its §3 hypothesis is **retired**: swap share does not predict template status, at any aggregation. Two of its own premises were corrected in place: the §1 dichotomy is not exhaustive (the fund is half the weeks and **two thirds of the contracts**), and the "median 0.993, no asymmetry" reading it inherited from `§B31` is direction cancelling. §A.2 now works a **real** market, live cattle, pinned by `tests/test_appendix_live.py`. Contract master deliberately **not** started. Executed 2026-08-02 on `claude/template-followups-doc-corrections-45de1d` and **never pushed**, which is what `2026-08-03-b-series-recovery.md` §1 recovered and §2 landed |
| [2026-08-03-index-share.md](2026-08-03-index-share.md) | **COMPLETE as far as it can go, 2026-08-03.** The only handoff here authored in another repo (cotdata#97, moved here as PR #36; a second session filed a duplicate copy before checking the remote, which is what the claim-first convention below exists to stop). §1: **the premise is retired, exactly as §4 anticipated.** Index positioning is not meaningfully stickier than swap. Swap is the MORE persistent at 12 weeks (median 0.826 vs 0.777, index ahead in only 4 of 13), index is 14% steadier week to week, and **under the worst 5% of weeks the swap book moves less than the index book** (-0.00167 vs -0.00336), adding to net long in 3 of 13 where index never does. §2 was recorded as blocked on **§B33-B36**, and re-derived blind as **`2026-08-03 §C1-C4`**. **Both readings are now corrected** (`2026-08-03-b-series-recovery.md` §2, and this file's appended §7): §B33-B37 **existed all along**, on an unpushed branch, so 08-02 closed at **B37** rather than B32 and §2 was in fact executable in full a day before it was called blocked. The cited figures were **not invented**: "22 of 39" and "17 in both halves" reproduce exactly. Of §2's three headline figures one **cannot respond to `w_SD`** (the shape rule reads two nets and their signs), one (`A_agnostic`) is **defined by `§B34` at a median of 3.0237**, not undefined as §C4 first read it, and `Q_sell`/`Q_buy` answers: median `A` swings **42.0% on the 13 Supplemental markets** against 0.6% pooled, so **`w_SD` is load-bearing exactly where the handoff cares**. Unsought finding: swap sits at 0.305 of Managed Money on routine turnover and 0.067 under stress, so `swap: 0.4` is incoherent **between regimes, not between markets**. **Metals are permanently outside coverage, so most of §0 is still open.** Weight table unchanged |
| [2026-08-03-swap-dealer-weight-decision.md](2026-08-03-swap-dealer-weight-decision.md) | **COMPLETE 2026-08-03 (PR #44), decided by the human as option (a): `swap: 0.4` stands.** Its §4 released exactly one piece of work and that work **corrected the handoff's own §1**: `single_weight_sweep` shipped, and re-swept over the order-preserving band it showed §1's 42.0% was the wrong BAND rather than wrong arithmetic. Honest figures **17.9% on the 13 Supplemental markets, 3.5% pooled** over `w_SD ∈ [0.2, 0.4]` (`§C10`). **Decided in parallel with `§C6-C8`, which landed as PR #43 while this was in flight and reached the same place by a different road**: §C6 closed option **(c)** by settling static weights independently, §C7 measured the cost of (a) as a **+19.6% median `Phi` inflation, worst on gold at +27.8% (2.30x cocoa) and precisely during stress weeks**, and §C8 showed most of that never reaches `D`, because the composite consumes a percentile and the median market-week moves only 5.9 of them. **§C8's split is the operating rule: on classic outrights the band is a footnote (Spearman 0.954, 18 of 20 top markets in common); on the ERCOT and PJM book it can invert a market's own history (Transco Zone 6 at -0.416), so publish the band there.** Two independent measurements, one decision, and no re-litigation. Option (c) is unscheduled rather than closed; metals remain permanently outside coverage |
| [2026-08-03-b-series-recovery.md](2026-08-03-b-series-recovery.md) | **COMPLETE, outcome as §7 and §8.** §1 recovered `claude/template-followups-doc-corrections-45de1d` (`11b7c81`), local-only with 2,070 insertions, by pushing it unaltered. §2 landed its content on `main` by **porting rather than merging** (the branch predates six merges and would have reverted `flow.py`, `check_skips.py`, `conftest.py` and the whole 08-03 lineage), so **that branch should be CLOSED unmerged**. `amendments-2026-08-02.md` is now B1-**B37**: it never closed at B32, it closed on a branch nobody could see. **§C4 is superseded**, having read `A_agnostic` as WEIGHT-agnostic where `§B34` defines it as DIRECTION-agnostic at a median of **3.0237**; corrected in place with both readings pinned. The cocoa split point is **resolved**: same date, 2025-10-21, differing only on which half that week falls in, and every other figure identical under both rules. §3 changed the citation convention to **path plus reproducer** and made the bare form fail loudly (`tests/test_references.py` over 368 references in 42 files; one unresolved at the time, `§C5`, recorded rather than deleted and then forced out by its own guard when #42 merged). §4 ran the band `w_SD ∈ {0.067, 0.2, 0.4, 0.7}` under a **settled static-weights decision** and filed `2026-08-03 §C6-C8`: `w_SD = 0.4` overstates fragile capital on 99.31% of market-weeks by a median +19.6%, **gold at +27.8% is 2.30x cocoa at +12.1% so the prediction holds**, and 0.067 moves the ceiling 10.0 to 14.925 so the band is not on one scale. Most of it never reaches the composite, which consumes `pct(Phi)`, except on **98 of 264 markets** where the two tables reorder a market's own weeks. **Weight table unchanged** |

| [2026-08-03-step2-contract-master.md](2026-08-03-step2-contract-master.md) | **COMPLETE 2026-08-03 (PR #46). Task 1 executed, gate PASSED; Tasks 2-3 found already shipped.** The largest finding is procedural: Tasks 2 and 3 ask for `contract_master.py`, `notional.py`, `riskunits.py` and `extremity.py`, **all of which already existed**, so this is the third instance of the duplicate-build failure this README exists to prevent and the first where the duplicate was requested in writing. Only two Task 2 items are genuinely unbuilt and both are data-blocked (price limits, and §379's OI migration per `2026-08-02 §B19`). The gate passes decisively: **25 of 25 covered markets are real outright, 0 are power/gas/carbon**, all **7 of 7** always-template markets are inside, and Managed Money prominence is **0.1371 covered against 0.0370 uncovered**. Two premises corrected: the universe is **45 markets across two report types, not 25** (`§C12`, the handoff counted only Disaggregated; CFTC publishes no financials there, and 26 + 21 = the whole 47-symbol spec table), and the uncovered 254 is **three populations, not two** (`§C13`, `§C14`: 213 certificates / **7 differentials** / **34 real outright**), where the differentials are excluded because the ladder computes a position value and a spread does not have one. Backlog is 34 codes but ~23 instruments, headed by ICE Europe WTI; **micro gold should be settled first** as it duplicates a covered market, per `2026-08-02 §B30`. `§B33`'s cited 58.1% is a different population from the covered energy outrights, which measure **51.2%**. `rank_markets` alignment was **NOT** structurally closed and now is (`§C11`) |
| [2026-08-03-report-layer.md](2026-08-03-report-layer.md) | **COMPLETE, all three tasks. §2 (PR #55) gate PASSES; §3 shipped (PR #56); §4 shipped (PR #59).** **§4 ran despite §9 of the handoff saying not to, and §9's reason was half right** (`2026-08-03 §C28`). `§C23`'s measurement stands, the obligation IS vacuous and no panel today carries both a `pct(D)` and a market `§C8`'s rule names. What did not follow is that the classification is therefore worthless: the obligation addresses whoever publishes a band and has no audience, while the classification answers what a reader holding one `D` asks, **does the band bind here**, and `no, this is a classic outright` is information rather than an absence. `§C22` had drawn that distinction one section earlier and it was not applied. Shipped as `futures/stratum.py`, and the promotion is **verified rather than asserted**: the moved classifier reproduces `§C13` and `§C14` exactly, 213 certificate / 7 differential / 59 outright on the vintage latest week and 27 of 27 outright on current-state, down to the individual differential code. The brief now carries **three of five** reading instructions; the remaining two (`§A21`, `§A22`) were shown un-carryable by measurement rather than left unbuilt, so three is a floor. `test_stratum_live.py` **fails on purpose** the week the vintage panel reaches `min_periods`, which turns §9's "re-check after 2026-12-29" into an automatic announcement rather than a date someone has to remember. Reproducer [`../analysis/reproduce_stratum.py`](../analysis/reproduce_stratum.py). **§3's answer, before §4 moved it, was that a `D` can carry ONE of its five reading instructions and must NAME the other four**, which is §5's negative #4 closed by its escape clause rather than by prevention (`2026-08-03 §C25`). Three of the five were then not carryable per row **for measured reasons and not for want of effort**: `§A21` is identical on every row, `§A22` is a property of a pooled ranking under a weight sweep, and `§C3` was a property of a population **until §4 gave it a carrier** (`§C28`), which is the one of the three that moved. The fourth, `§B2`, attaches on 100% of rows and **the composite chain does not attach it**, so the obvious assembly carries one and the ledger's declaration is the only thing that says the fifth is missing rather than inapplicable. Shipped as `futures/brief.py` (pure selection, computes nothing) over two derivations in `composite.py`, which owns `D`: `add_score_state` (the `E = 1` gap) and `add_unwind_state` (the `§A17` carrier). Two findings §9 did not anticipate: `indeterminate` has **two** causes and the larger one (8,856 rows with no prior scored week, against 5,118 with a silent flow state) was the unmeasured one (`§C27`); and **a third of all possible briefs have no number at all** (8,831 of 27,194), every one of which previously rendered as a blank cell beside three populated factor columns (`§C26`). Reproducer [`../analysis/reproduce_brief.py`](../analysis/reproduce_brief.py). Earlier outcome as §9, findings `2026-08-03 §C20-§C24`, reproducer [`../analysis/reproduce_report_gate.py`](../analysis/reproduce_report_gate.py). The gate lands at **`R=4, E=1` strict and `R=5, E=2` lenient**, and **both readings pass**, so the verdict does not turn on the one judgement call the author had an incentive to resolve. Three findings change what §3 should be. **§4 was recorded dead here and is not** (superseded by `§C28`, above), though not for the reason the handoff gave either (`§C23`): the stratum was never the blocker, `venue` parses from `market_name` on 100% of rows, but the current-state panel has 1,051 weeks and **zero** power/gas markets while the vintage panel has **263** and only 82 weeks against a 104-week `min_periods`, so `§C8`'s rule is vacuous until **2026-12-29** at the earliest. **The `ΔD` hypothesis was half wrong** (`§C21`): `flow_state` conditions `ΔD` strongly at the extremes (`long_liquidation` falls 84% of weeks, `new_longs` 15%, opposite median signs) but `quiet`, the "genuinely safer" arm the handoff named, occurs **3 times in 27,167 market-weeks and never on a falling-D week**, and `mixed` covers 58.2% of falling-D weeks at a median `ΔD` of exactly 0.0000. **`E=1` is the number to carry**: three of four survivors are already attached by a shipped function, so the brief closes one genuine gap and is otherwise convenience. Original text below preserved. Drafted against `075ad26`. §13 step 7's report layer, which is the last build-order item not blocked on data. **Gated**: §2 asks whether the caveats a reader needs before reading a `D` are properties of a market-week at all, or only of the panel and the construction, and §3-§4 run only if enough of them are row-computable. The concrete unowned obligation is `2026-08-03 §C8`'s operating rule (publish the `w_SD` band on power and gas basis markets, where Spearman between the two weight tables runs to −0.4160), which **nothing in `src/` can apply** because the classic-outright stratum lives only in `docs/analysis/reproduce_template_stability.py`. **Both thresholds are pre-registered rather than judged on the day**: §2 carries an `R`/`E` table over its seven caveat candidates that routes two of four outcomes to a stop, and §5 names **four** negatives, the fourth being the partial brief that carries some caveats and silently omits others, which is the likeliest and the most dangerous because it looks complete. All four close it honestly. §1 inventories what is already shipped, because a report-layer handoff is unusually exposed to the duplicate-build failure that PR #46 hit. **§8 appended:** its §1.2 cites a live worktree as proof the spec backlog was taken, and that session merged as PR #47 two and a half minutes before this one, so the claim now points at [2026-08-03-spec-backlog-producer.md](2026-08-03-spec-backlog-producer.md) instead. §8 also carries `§C15` forward into §2, since "a value that correlates with the right answer is not a value that carries it" is exactly the inference the gate has to make. **§8 itself was then corrected**: it led with `§C15`'s level correlation of Managed Money net, which `2026-08-03 §C16` measured as noise (an independent random walk posts a max level `r` of 0.773 half the time), so it now leads with `r(ΔMM)` against a differenced noise band. The conclusion is unchanged and the lesson gained a second layer |

| [2026-08-03-spec-backlog-producer.md](2026-08-03-spec-backlog-producer.md) | **OPEN, blocked on the Norgate producer (Windows box). Nothing to do in this repo.** A work order to spec the five largest §C14 backlog codes, ICE Europe WTI (`067411`) and the four Henry Hub codes, taking coverage 45 -> 50. Filed as a claim so the analysis is not redone. **It also records an objection this session raised and withdrew** (`§C15`): these look like variants of covered `CL` and `NG`, so the micro-gold argument was applied to them, and it fails on measurement. Open interest tracks (WTI 0.771), which is what makes them look like duplicates, but **Managed Money net is NEGATIVE in all five** (to -0.643) and flow correlation is near zero, so they are a different holder base rather than a second copy. The general lesson narrows `§C14`: **test positioning correlation before merging or excluding a variant code**, since a shared underlying is not sufficient grounds for either. Micro gold is unaffected and still wants merging. The blocker is structural, not effort: all five need a `contract_specs` row plus **both** stored price tiers (`propadj` is derived from the pair), and **MME/MFS are the worked example** of a registry entry without them, invisible to coverage. The one genuine unknown is whether Norgate carries the three cash-settled Henry Hub look-alikes; §4 says report vendor-absent rather than substituting a proxy. **§8 appends a SECOND TRANCHE, the ag and dairy backlog, and its answer is mostly "do not" (`§C17`): six of ten should not be specced at all.** Only **rough rice** (`039601`) clears every bar, on MM share 0.433, which is 3.2x the covered median and the highest of the ten, and it is the SMALLEST of them, so §C14's by-open-interest ordering is close to backwards. **The dairy block minus cheese is a refusal, not a deferral**: Managed Money runs 1.2% to 3.5% of OI (106 to 204 contracts), so there is no fragile holder for the fragility term to describe, and adding them would fail `§C13`'s gate from the inside. **`§C16` corrects `§C15`, and §8.3 corrects §2 of the handoff itself**: positioning LEVELS correlate spuriously (lag-1 autocorrelation 0.956; an INDEPENDENT random walk scanned over the covered 25 posts max \|r\| of **0.773 half the time**), so §2's bolded negative level correlations are noise. Both conclusions survive on the first-differenced figures each also reported, which is luck rather than method. **Variant tests use first differences against a noise band from the same panel.** **§9 records the human's decision cutting tranche 2 to `039601` rough rice ALONE**, dropping WHEAT-HRSpring, canola and cheese, all three on flow correlation against an already-covered market rather than on size or effort. **The producer ask is therefore SIX codes**, and §9.2 supersedes §5's counts: 45 -> 50 -> **51** covered, `joinable` 47/49 -> 52/54 -> **53/55**. Rough rice is in **82 of 82** weeks so it lands in the latest-week count, unlike oats. If the run is split, **do rough rice first**: Norgate `ZR` is a standard CBOT contract so §4's vendor risk barely applies, and it is the one backlog item both certain to be available and certain to be worth having. If any dropped code is revisited it should be WHEAT-HRSpring, re-running `§C17`'s test rather than arguing from open interest. **§10 runs the test over the remaining 24 metals and energy codes and proposes NO third tranche.** It first had to fix the test: `§C20` found §C17's flat 0.05 bar condemned **four of the five committed tranche-1 codes**, because it benchmarked against the pooled covered median 0.1371 when **covered ENERGY sits at 0.0435** and Nat Gas (0.0369) and WTI (0.0399), the two largest markets in the universe, fall under it. Benchmarked **within complex** all six committed codes pass (0.70x to 2.02x energy; rough rice 4.41x grains, the strongest anywhere), so nothing decided needs revisiting. `§C21`: **13 of 34 pass**, and seven uncommitted ones are recorded rather than requested, led by **`06665P` Mt Belvieu ethane at 3.80x**, which the flat bar would have hidden. **The dominant failure mode is not redundancy: 16 of 34 fail for want of a levered holder against 5 for duplicative flow.** `06765A` WTI FINANCIAL has 175,418 mean OI, enough to rank 15th of the covered 25, and a median Managed Money net of **475 contracts**; two NGL codes sit at exactly zero. **Micro gold fails BOTH bars** (0.07x, flow 0.355 vs gold), settling `§C14`'s advice by measurement. Second pooled-threshold error of the session after `§C16`: **a bar from a pooled median is a bar for the median complex and no other** |

## A handoff can also be a claim

Two modules were built twice in one afternoon because each session started the obvious next
piece without re-checking. Writing the claim here before starting is the fix, and it costs a
few minutes against a duplicated module. A `claimed, not started` handoff says what is being
taken, what it is blocked on, and what decision it needs, so another session can object before
either of us has written anything.

## How this differs from the neighbouring directories

| Directory | Lifecycle |
|---|---|
| `design/` | **living.** Amended as measurements land. The current best statement of how the system works |
| `handoffs/` | **append-only.** A dated work order plus its outcome. Never revised in place |
| `analysis/` | **point-in-time.** Output computed against a named report week, never amended. A later week gets a new file |
| `adr/` | **immutable once accepted.** Superseded by a new ADR rather than edited |

The distinction that matters most is `design` versus `analysis`. A design doc that says
something the data disproved is a bug to fix; an analysis document that says something later
weeks disproved is a correct record of what was true then. Editing the second to match the
present would erase the evidence that anything changed.
